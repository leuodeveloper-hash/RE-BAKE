/**
 * 피그마 디자인 시스템의 간격(Spacing) 상수
 * 피그마에서 사용하는 간격 값을 여기에 정의하세요
 */

export const Spacing = {
  xs: 4,
  sm: 8,
  smd: 12,
  md: 16,
  lg: 24,
  xl: 32,
  xxl: 48,
  xxxl: 64,
};

export type SpacingKey = keyof typeof Spacing;
